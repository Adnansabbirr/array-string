/* ===========================================================
This time we learn upper & lower case & comparison with string 
=============================================================*/
const savedUserName='SabbiR';
const userName='sabbir';
if (savedUserName.toLowerCase() == userName.toLowerCase()){
    console.log('user exist');
}
/* We can convert also upper case by calling function .toUpperCase() */